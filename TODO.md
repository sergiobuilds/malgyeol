# 말결 구현 TODO

> 실행 순서만 적은 작업 목록입니다. 제품 방향·범위·상태의 정본은 `docs/MASTER-MAP.md`입니다.
> 프론트 전환은 전화와 실제 업체 주문이 끝날 때까지 후순위로 둡니다.

## 현재 결론

- 제품: 스마트폰과 외출이 어려운 재가 어르신이 일반 전화로 승인된 식사·식품·생필품 지원을 요청하고, 수행기관의 제공·수령까지 기록하는 서비스 이행 채널.
- 기본 런타임: `src/server.ts` → `src/careApp.ts` → 결제 없는 돌봄 요청·수행기관 상태 전이.
- 기존 주문 경로: `src/app.ts`와 `src/food-support/`에 전화 입력·공급자 카탈로그·실주문·readback 기술검증이 남아 있으나 현재 기본 서버에는 연결되지 않음.
- hygiene 결론: 제품이 무엇인지, 현행 구조가 무엇인지, 레거시가 무엇인지 확인 완료. 이후에는 구현·실측 증거를 만든다.

## P0. 전화 대화 복구

- [ ] 기본 런타임의 전화 경로를 하나로 확정하고 실제 배포 진입점에 연결한다.
  - 수신 → 지정 시나리오 발화 → 요청 이해 → 자연스러운 재확인 → 1번 승인 → 요청 생성.
  - 현재 로컬 경로는 `/voice/incoming` → `/voice/request` → `/voice/confirm`까지 동작한다.
  - 실제 음성 해석·전화 대화 경로는 기존 `src/voice/twilioRoutes.ts`·`src/food-support/phoneFoodCoordinator.ts`와 연결 여부를 결정해야 한다.
- [ ] 지정 시나리오를 한 가지로 고정한다.
  - 예: “납작보리쌀 한 봉지 필요해요” → 상품 확인 → 필요한 확인 질문 → 이용자 승인.
  - 신규 자격·주소 변경·위험 발화는 자동 실행하지 않고 담당자 확인으로 넘긴다.
- [ ] 등록된 서비스 전화번호의 사업자 라우팅·자격정보·웹훅 URL을 확인한다.
- [ ] 실제 전화 1회 이상을 실행하고 다음 증거를 보존한다.
  - 통화 시각·Call ID
  - 개인정보를 제거한 대화 기록 또는 녹취
  - 승인 전 요청 미생성
  - 승인 후 `caseId` 1개 생성
  - 재전송·취소·계획 밖 요청의 무실행 결과

## P1. 실제 로컬업체 주문

- [ ] P0의 승인된 `caseId`를 기존 공급자 주문 어댑터까지 끊김 없이 전달한다.
- [ ] 지정 상품을 실제 로컬·도매업체 카탈로그에서 조회한다.
- [ ] 이용자 확인 뒤 업체 POST를 정확히 1회 실행한다.
- [ ] 업체 주문번호, 상품·수량, 업체 응답, readback 상태를 같은 `caseId`에 기록한다.
- [ ] 동일 전화 재시도·웹훅 재전송·네트워크 timeout에서 중복 주문이 생기지 않음을 실측한다.
- [ ] Sergio가 확인한 실제 납작보리쌀 주문 결과를 개인정보·비밀정보 제거 후 재현 가능한 증거 파일로 남긴다.

## P2. 하나의 사건 기록과 예외 이관

- [ ] 전화 요청과 업체 주문을 하나의 사건 원장으로 통합한다.
  - `REQUESTED` → `CONFIRMED` → `SUPPLIER_SUBMITTED` → `SUPPLIER_CONFIRMED` → `PROVIDED` → `RECIPIENT_CONFIRMED`.
- [ ] 업체 품절·timeout·미수령·계획 밖 요청·위험 신호를 담당자 업무함으로 이관한다.
- [ ] 정상 건은 담당자 재입력 없이 종결하고, 예외만 담당자 처리 대상으로 남긴다.
- [ ] 현재 인메모리 저장소를 실증 전 영속 저장소로 바꾸고 멱등성·재시작 복구를 검증한다.

## P3. 운영·증거 패키지

- [ ] 전화번호·웹훅·업체 인증정보·환경변수 설정을 운영 문서로 정리한다. 비밀값은 저장하지 않는다.
- [ ] 시작·health check·로그 확인·재시작·장애 시 수동 전환 절차를 정리한다.
- [ ] 최종 증거 묶음을 만든다.
  - 실제 전화 대화 증거
  - 실제 업체 주문·readback 증거
  - `caseId` 사건 이력
  - 중복 방지·예외 이관 결과
  - 실행 명령과 검증 결과
- [ ] P0~P3 완료 후에만 `docs/MASTER-MAP.md`의 leaf 상태와 완료 증거를 갱신한다.

## 후순위

- [ ] 이용자·수행기관·담당자 프론트 전환
- [ ] 3분 데모·발표 화면·접근성 보정
- [ ] 실증 제안서와 발표 자료의 최종 다듬기

## 완료 기준

- [ ] 실제 전화가 지정 시나리오대로 대화하고 승인 후 한 번만 접수된다.
- [ ] 같은 `caseId`로 실제 업체 주문이 한 번만 생성되고 업체 readback이 확인된다.
- [ ] 예외는 자동 주문하지 않고 담당자에게 이관된다.
- [ ] 전화·주문·제공·수령·예외 증거를 한 패키지로 재현할 수 있다.
- [ ] `npm run verify`가 통과한다.
